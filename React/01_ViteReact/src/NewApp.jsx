// if we create a react app by using 'create app' then we can use .js but since we are using 'vite react' then we need to use .jsx extension
// one most important is that the function name should start with an uppercase.

function NewApps() {
    return (
        <h2>React app with vite</h2>
    )
}

export default NewApps;