#include<stdio.h>

int main() {
    // write a c program to check if a number is divisible by 97 or not
    // int num;
    // printf("Enter a number: ");
    // scanf("%d", &num);
    // printf("divisibility test: %d", 97%num);

    // Evaluation
    int x = 2, y = 3, z = 3, k = 1;
    printf("The value is: %d", 3 * x / y - z + 1);

    return 0;
}